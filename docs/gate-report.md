# Acceptance gate report

Generated by `npm run gates`. Every row is measured on the committed
catalog, so two runs on two machines produce the same numbers.

Run at 2026-08-31T07:26:24.411Z.

| Epic | Requirement | Measured | Result |
|---|---|---|---|
| E1 — exact-match precision | ≥ 99% on a 500-pair set | **100.0% precision (185 correct of 185 rendered; recall 100.0%)** | pass |
| E1 — silent variant substitution | zero across a 10k-run fuzz | **0 violations in 10,000 runs (18,236 matches rendered, 2,860 of them tier 2)** | pass |
| E2 — query field accuracy | ≥ 90% field-level on 1,000 queries | **100.0% overall (brand 100.0%, articleType 100.0%, colour 100.0%, gender 100.0%)** | pass |
| E3 — match latency | p95 ≤ 120 ms | **p50 0.01 ms, p95 0.01 ms, p99 0.02 ms over 5,000 calls (budget 120 ms)** | pass |
| E8 — wishlist leakage | no wishlist field in any unauthenticated response or log line | **0 leaks across 66 calls covering responses and shadow records** | pass |
| E13 — multi-match ranking | cap, one slot per product, usefulness ordering, stable order | **0 violations across 648 queries (283 rendered, 107 multi-match, 0 unstable)** | pass |
| DC — signal provenance | every signal cites a source; every generated field is labelled as such | **0 sourceless and 0 mislabelled across 3,540 signals in 354 states** | pass |
| CR-05 — comparison staleness | every changed compared item is marked, and no unchanged one is | **0 missed and 0 false alarms across 400 churn runs that moved 780 items** | pass |
| C-7 — accessibility | every control labelled, every touch target ≥44pt across the Part A and Part B surfaces | **0 unlabelled and 0 undersized of 52 controls across 6 surfaces** | pass |
| Pairing — cross-category coherence | no suggestion crosses gender, repeats or clashes a slot, resurfaces a bought or bagged item, or comes from outside the wishlist | **0 violations across 1,535 suggestions from 535 seed products; 427 of them produce a look, averaging 3.59 distinct categories each (108 at 0, 173 at 3, 254 at 4)** | pass |

## What each number is not evidence for

- **E1 — exact-match precision** — Labels are generated, not hand-labelled. The generator derives brand and article type the same way the matcher does, so an error shared by both is invisible here. This measures matcher behaviour given correct fields, and guards against regression; it is not the Phase 1 exit evidence.
- **E1 — silent variant substitution** — Fuzzes stock and query against the thirty committed wishlist items. It does not vary the wishlist itself, so a substitution that only occurs for a saved item shape absent from this fixture would not be caught.
- **E2 — query field accuracy** — Queries are assembled from catalog values, so they are well-formed by construction. Real queries carry typos, plurals, slang and brand misspellings that this set contains none of; treat the number as an upper bound.
- **E3 — match latency** — In-process JavaScript on one machine with the catalog already in memory. It is not the 500 rps load test the plan asks for, and excludes network, serialisation, queueing and database access. It shows the matching work is not the bottleneck; it does not show the service meets its SLO.
- **E8 — wishlist leakage** — Covers the response payload and the shadow topic. It cannot see leaks through timing side channels, through a real service's access logs, or through any surface this prototype does not have.
- **E13 — multi-match ranking** — Asserts the properties the ranking guarantees, not that the ordering is the one users would prefer. Which of three valid orderings performs best is a question for the experiment, and it has not been run.
- **DC — signal provenance** — Checks that a source string exists and that the synthetic flag matches the known generated fields. It cannot check that a source is *true* — that the delivery date really came from this seller's serviceability, say — only that one is claimed and that generated data is never presented as real.
- **CR-05 — comparison staleness** — Only stock churn and withdrawal are exercised, because they are the only things the inventory simulator can move; price, seller and returns are catalog-static, and delivery changes only when the pincode does. A pincode-driven change is covered by unit tests rather than here.
- **C-7 — accessibility** — Static assertions over rendered props, and a floor rather than a proof. Width is only checked where an element declares one: React Native stretches a column child by default, so a static check cannot tell a self-sizing pill from a full-width row without seeing the parent. It also does not test focus order, colour contrast, or what a screen reader actually announces, and it cannot run VoiceOver or TalkBack — the plan's manual pass is still required and is not replaced by this number.
- **Pairing — cross-category coherence** — Coverage is bounded by what the shipped wishlist contains — thirty items, of which five are shirts and two are excluded by the lifecycle gate. The saved wardrobe filled the accessory slots for men and women, which nothing saved reached before; whole slots still go untested because nothing saved fills them: no adult bottomwear for women, and no kidswear at all — which is also why 108 of the seeds here are dressed in nothing, and why the density figure is over the seeds that produce a look rather than over all of them. A pass means no wrong pairing among the pairings this data can produce, not that the engine is correct over a catalog it has never seen.
